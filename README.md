# School Register System

A web application for managing school attendance built with Streamlit.

## Features
- Daily attendance tracking
- Student management
- Monthly reports
- Class registers

## Setup
1. Install Python 3.8+
2. Run: `pip install -r requirements.txt`
3. Run: `streamlit run app.py`
